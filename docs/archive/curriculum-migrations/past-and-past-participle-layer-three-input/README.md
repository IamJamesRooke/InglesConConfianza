# Layer-Three Input Snapshot

This snapshot preserves the complete 107-file teaching tree that immediately preceded the flatter transformation-centered organization. The live curriculum is canonical; this folder exists only as a migration safeguard until later curation explicitly removes it.

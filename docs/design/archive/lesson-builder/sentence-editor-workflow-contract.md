# Sentence editor: approved implementation contract

This is a bounded implementation of the owner's approved sentence-writing workflow, superseding conflicting old hint-panel/semicolon-only/Tab-to-button instructions. No new redesign. Authoring only, preserve learner behavior, data model, IDs, existing accepted-answer arrays, history/persistence and prior table/header/geometry work. Real web/data/lessons.json is owner-dirty: NEVER mutate during tests. No DB writes/localhost3000 changes. Claude only, no browser extension, no auto commits/subagents/dependencies.

## Appearance

Rest: composed Spanish then English (first accepted answer per piece), optional authored instruction, natural wrapping; 16px teaching text and consistent line-height across resting/active sentence fields. No hints/counts/placeholders/toolbars/boxes or minimum control height. Preserve language colors/author emphasis. Hover reveals only existing upper-right slide drag/duplicate/delete, no text/background shift.
Active: aligned content-sized bilingual pieces with compact gaps. Focus outline on edited field, no permanent boxes per piece or full-width management toolbar. One Add pair at end for mouse access. Hint input and selected-pair delete are contextual and must not stretch Spanish/English widths. Add instruction remains one quiet optional action. No empty instruction without request.

## English alternatives: one field, no secondary panel

Editing English displays ALL stored answers in one field joined by ` / `. Local draft must stay stable while typing; parse ONLY on blur/Enter/Tab/Escape commit through existing history/persistence semantics. Remove alternatives button/count/panel. Render first stored accepted answer at rest. Trim/ignore empty entries and EXACT duplicate answers on commit, preserve order/case differences. Support slash entry AND legacy unescaped semicolon entry. Define/test backslash escaping: literal slash/semicolon/backslash roundtrip; existing strings are formatted with escaping when loaded into editor, NEVER blindly split stored data. URLs/fractions with literal slash must survive format/parse unchanged. No automatic DB/JSON migration, no eager rewriting on mount or mere focus/blur of unchanged data. Clearing field supported with stable empty-input representation. No parsing mid-IME. Existing individual mutation callbacks may be composed minimally, but don't drop extras/duplicate entries when array lengths shrink/grow. Undo grouping must treat field commit coherently.

Parser API owned by builder-structure: NEW web/src/lib/lesson-builder/answer-entry.ts exports formatAnswerEntry(answers: readonly string[]): string and parseAnswerEntry(value: string): string[]. Format escapes delimiters/backslashes then joins ` / `; parse accepts both unescaped slash and semicolon, removes defined escapes, trims/dedupes exact values; empty returns []. UI owns mapping [] to editable blank representation if data requires it. Tests for parse(format(stored)) preserve literal content (other than intentional normalization on actual edited commit). Do not alter canonical stored answers just to hide an old malformed display; flag ambiguous legacy delimiter strings for parent.

## Direct sentence hints

Extend the approved table pill entry to selected SENTENCE pair: one quiet pill-shaped input under/adjacent selected pair, Hint placeholder and accessible owner label. No lightbulb/Add hint/Edit hint/remove-hint panel. Existing hint uses same input, clear removes it. Pill contains authored hint ONLY; no source phrase prefix. Existing hints may show while editing, no empty pill for every unselected pair. SENTENCE resting hints remain hidden; TABLE resting authored pills remain visible per table contract. Local draft/blur commit/IME supported; hints don't expand pair columns.

Alt+ArrowDown from sentence Spanish/English field focuses its hint. Escape from hint commits and returns to exact originating field/caret without exiting slide. Verify via browser and avoid interception during composition. Retain normal focusable keyboard access if OS captures shortcut; no claim universal OS compatibility. Shortcut in tooltip/on-demand reference, not banner. No modification of learner hint/reveal behavior.

## Writing navigation

Enter on resting slide activates. Tab Spanish -> English -> next pair Spanish. Tab from LAST COMPLETE pair creates EXACTLY ONE pair and focuses Spanish; reopening existing slide works identically. Reuse existing empty trailing pair. Empty/incomplete trailing pair must not grow indefinitely or trap keyboard: Tab from its English continues to available tools/outside; Shift+Tab never creates. Existing arrow/caret/selection semantics unchanged. Escape nested tool first; otherwise commit local draft, exit editing, focus resting slide without automatic reactivation. Deleting pair retains recovery and accessible owner name; slide deletion separate.

## Insertion keyboard UI

Ctrl+Alt+Enter unchanged: show insertion after active slide. Order everywhere Explanation (E), Sentence (S), Table (T), initial keyboard focus Sentence. Press E/S/T inserts immediately ONLY while palette has focus, not during content typing. Arrows choose, Enter inserts/focuses first writing field, Escape restores original caret. Keyboard-invoked palette shows key badges and small nearby escape/help cue; ordinary hover doesn't print shortcut badges. Remove distant oversized contextual keyboard banner, retain compact on-demand shortcut reference. Update exact help/ARIA/tests; no stale V/table binding or 'next slide' label. Do not change other language-formatting shortcuts.

## Ownership/delivery

builder-presentation owns sentence-editor.tsx/presentation, lesson-document.tsx, slide-insert-control.tsx, minimal editor focus/help/HUD helpers, lesson-library-document.css/authoring-base.css, focused editor tests. Do not edit parser helper/test file or site-header/module/nav/library components. builder-structure owns ONLY new parser helper and its dedicated new unit test. Parent handles any cross-file request. Both preserve dirty prior work. Parser static/unit only; editor has isolated browser slot after source changes settle. No running full suites/builds against owner caches. Prior table/header surface changes still need browser checks; report what is actually verified.

Checkpoint 1: parser + sentence local draft/Tab/direct hint flow, concise status. Checkpoint 2: insertion help/order + focus/visual checks. Do not add unrelated half-width, Add lesson relocation or global type-scale work to this batch; pending separately. ~20min checkpoint, stop/report repeated failing test rather than unlimited retries. Final handoff /tmp/sentence-workflow-handoff.md per requirement PASS/BLOCKED + exact files/checks/screenshots, processes cleaned. Parser /tmp/answer-entry-handoff.md.

Acceptance: isolated keyboard-only create lesson/title/Enter explanation, formatting, CtrlAltEnter/S sentence, Spanish/Tab/English multiple alternatives/Tab new pair, optional hint via AltDown/Escape, leave/reopen, edit committed answers, duplicate/delete/undo/save/reload; all three insertion types, empty/incomplete/ShiftTab/IME; sentence rest no hint, table hint preserved; 16px computed rest/edit, narrow wrapping/no overlap, Escape no accidental reactivation. Preserve artifacts. Full keyboard reordering/history regression if touched; report preexisting gaps rather than expanding scope.

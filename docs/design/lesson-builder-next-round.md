# Next round — frozen owner-approved corrections

Supersedes conflicting older placement/alignment/type instructions. Preserve existing uncommitted application work and owner-dirty web/data/lessons.json. Old builder-presentation STOPPED and relinquished files: /tmp/sentence-workflow-stop.md records incomplete browser verification. No real data/DB writes, owner server restart/build/cache changes, learner redesign, dependencies, subagents or auto commits. Claude Sonnet only. NO browser extension. Agents SOURCE + narrowly focused static/unit checks ONLY this round; parent owns browser integration AFTER all source writes stop. No test/port retry loops.

## Track S — structure (builder-structure)

S1 Fix root responsive module width, including stacked half-width layout. Prior flex:1 1 0 fix did not cover owner cases. Inspect entire parent chain/align-items/breakpoints, not hardcoded width. At fixed viewport, document/module/header widths invariant across all lessons open -> all collapsed -> one open -> collapsed again, empty module and short content. Full/half/mobile. Own module-navigation.css for shell fix; if lesson-library-document.css needed request exact selector to parent, do not overlap Track E.
S2 Remove drag handle from DISPLAYED module header only; sidebar drag reordering stays. Remove dead header-only refs/handlers where safe. Preserve delete module and lesson actions/collapse.
S3 Bottom Add lesson is always-visible compact left-aligned button OUTSIDE module card with modest gap, no boxed full-width footer/divider. Between-lesson insertion shows '+ Add lesson' rather than bare plus on hover/focus, no reserved control row height. Keyboard/touch usable. Preserve exact position/create/focus/undo. Markup in lesson-library.tsx owned here; send exact new/existing footer selectors to Track E through /tmp/next-round-css-contract.md before they style. Do not wrap card in shrink-to-fit container; preserve list/drag semantics and width.
S4 Remove floating Ctrl . help trigger AND reserved space; shortcut remains, help accessible in EXISTING menu. Locate actual component, report if shared helper owned E before touching. Need real menu entry if absent, not fictional claim. Only add compact entry to current menu, no new help UI. Header style unchanged. Coordinate file ownership if keyboard-help.tsx touched; E must not touch it.

S owns lesson-library.tsx/module-navigator.tsx/module-navigation.css, needed existing menu/header/keyboard-help trigger TSX (not header theme), NEW structure unit tests. No document/editor TSX or lesson-library-document.css/authoring-base.css. Before edit list exact paths/checklist in /tmp/next-structure-handoff.md. ~15min checkpoint then stop with remaining blockers.

## Track E — editor/presentation (fresh next-editor)

E1 Typography: all teaching text explanation/sentence/table 16px regular 400, consistent line-height REST/EDIT. Remove automatic language-color bolding; preserve explicit strong/b/em/i authored content/formatting. Language color contrast unchanged. Headings modestly heavier, module title ~20px. Module header ~44–48px total via padding, usable actions not tiny. Concept metadata may stay smaller. No global font/color redesign; scoped builder selectors only, no learner change. Consolidate touched conflicting rules, not append new override layer.
E2 Explanation: gray width/text/padding same rest/hover/edit. Group three slide-action icons coherently upper-right with horizontal clearance; no detached icon/white patches, overlap, permanent row or vertical bloat. Preserve working formatting/selection.
E3 CENTER content-sized vocabulary table (supersedes left alignment); cells left aligned, same rest/edit column geometry, neutral faint row separators, no visible Spanish/English headings/blue perimeter/rounded outer box. Small 6–8px starting gap after table, not blanket 16–20px per slide. Preserve direct hint input and table resting hint behavior.
E4 Move whole concepts field (existing chips + Add concept) to lesson bottom AFTER slides and tail insertion; no new metadata footer/panel. Preserve tagging callbacks/keyboard/focus/save. Title -> content flow direct.
E5 Hint pills including selected empty/editing input PALE YELLOW readable dark text, no gray fill/pink/red border. Clear accessible focus, sentence rest no hint; table rest authored pill preserved. Don't change stored hint text.
E6 Remove detached pair trash control. Small X attached to pair upper-right OUTSIDE text, on pair hover/focus, accessible Delete pair label + owner; no reserved row or text shift. Keyboard/touch reachable, distinguish from whole-slide trash. Preserve undo and focus next/previous pair predictably. No destructive text shortcut.
E7 On actual Escape EXIT of sentence editing, prune pair only if Spanish, ALL English answers, hint are blank/whitespace. Check live drafts before prune, not stale props. Preserve partial text/hints. Empty slide stays, do not delete slide. Nested hint/palette Escape doesn't prune. Reuse existing mutations/history so save/reload doesn't revive pairs; no eager data cleanup on mount. IME unaffected.
E8 Apply Track S requested footer styling from /tmp/next-round-css-contract.md: module-bottom Add lesson OUTSIDE card, quiet compact button; between lesson + Add lesson reveal without reserved control height. No markup ownership overlap. If request absent checkpoint rather than guess structure.

E owns lesson-document.tsx/sentence-editor.tsx/sentence-presentation.tsx/explanation and table components if needed, lesson-library-document.css/authoring-base.css, NEW focused tests/helpers for blank-pair pruning if needed. No lesson-library.tsx/module-nav/header/keyboard-help.tsx/parser helper modifications. Existing /tmp/sentence-workflow-stop.md + /tmp/sentence-workflow-handoff.md give last work context: preserve new slash English local drafts, Tab-create, direct hints, E/S/T insertion. Do not redesign them or independently fix old Playwright specs yet. Old lesson data is dirty, never test against it.

## Checkpoints and evidence

Each track writes per-ID DONE/BLOCKED, exact files, actual static/unit commands/results, no 'browser passed' claim without run. First checkpoint <=20 minutes; no historical backlog/design additions. Parent owns existing failing UX spec updates and ONE coordinated focused browser pass after agents stop; tests must match approved features (module collapse removed, resting instruction text not textbox), not weakened to conceal failures. Preserve logs/traces and stop at representative repeated failure. Parent verifies width sequence, keyboard authoring, Escape blank cleanup/reload, table/typography/rest-hover-edit geometry, creation/tagging/recovery, menu help and screenshots at 1280/900/390px. Document any unresolved failures before commit. No more unbounded task queue.

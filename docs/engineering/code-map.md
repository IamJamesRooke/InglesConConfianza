# Current code map

Use this page to find the owner and the smallest useful read set for a routine task. The paths below are current implementation paths; this map does not describe proposed refactor destinations.

| Task | Start here | Stable symbols / boundary | Test command |
| --- | --- | --- | --- |
| Sentence keyboard entry | `web/src/components/lesson-builder/sentence-editor.tsx`, `lesson-document.tsx`, `web/src/lib/lesson-builder/builder-context.tsx` | `SentenceEditor` owns Spanish/English Tab flow and entry state, binding `lessonId`/`block.id` into `useLessonBuilder()` actions itself; `LessonDocument` supplies `lessonId`/`block` identity; `focusSlideWritingField` is in `web/src/lib/lesson-builder/focus.ts` | `npm run test:unit -- tests/unit/lesson-builder-logic.test.ts tests/unit/lesson-mutations.test.ts` |
| Slide insertion and keyboard help | `web/src/components/lesson-builder/slide-insert-control.tsx`, `keyboard-help.tsx`, `lesson-document.tsx`, `web/src/lib/lesson-builder/builder-context.tsx` | `SlideInsertControl` owns chooser navigation; `KeyboardHelpDialog` owns the dialog/map; `LessonDocument` owns insertion target/origin and calls `useLessonBuilder().addBlock` | `npm run test:unit -- tests/unit/lesson-builder-logic.test.ts tests/unit/lesson-mutations.test.ts` |
| Explanation formatting | `web/src/components/lesson-builder/explanation-editor.tsx`, `web/src/lib/lesson-builder/explanation-{schema,markdown,commands,e1,classifier}.ts`, `practice/practice-markdown.tsx`, `lib/lesson-builder/markdown.ts` | `EditablePracticeMarkdown` (Tiptap/ProseMirror) owns the editing surface; `parseExplanation`/`serializeExplanationDoc` own the markdown dialect; `setExplanationLanguage` + the editor registry are the only seam the keymap's `explanation` scope uses; `PracticeMarkdown`/`parseLessonMarkdown` stay the **learner-side** renderer (same dialect, separate parser) | `npm run test:unit -- tests/unit/explanation-markdown.test.ts`, `npx playwright test tests/ux/explanation-editing.spec.ts` |
| Save/retry and persistence | `web/src/lib/lesson-builder/use-lesson-persistence.ts`, `web/src/app/admin/lesson-builder/page.tsx`, `web/src/lib/lesson-builder/server/lesson-store.ts` | `useLessonPersistence` owns loading, acknowledgement, autosave/retry, request order, unload protection, and lesson deletion; `LessonPersistenceQueue` is the deferred-test seam; `mutateLessonFile` retains the server mutation queue | `npm run test:unit -- tests/unit/lesson-persistence.test.ts` |
| Script mode (E4) | `web/src/lib/lesson-builder/script.ts`, `web/src/components/lesson-builder/lesson-script-view.tsx`, `web/src/lib/lesson-builder/editing.ts`, `web/src/lib/lesson-builder/reducer.ts` | `parseScript`/`printScript` implement `docs/design/lesson-script-grammar.md` over the block model; `LessonScriptView` is the per-lesson `<textarea>` (`Ctrl+Alt+T`, `editing.ts`'s `scriptViewLessonId`/`setScriptView`); a clean parse dispatches `REPLACE_LESSON_BLOCKS` (`reducer.ts` → `applyScriptReplacement` in `script.ts`) as one undoable step | `npm run test:unit -- tests/unit/lesson-script.test.ts`, `npx playwright test tests/ux/script-mode.spec.ts` |
| Document history | `web/src/lib/lesson-builder/history.ts`, `reducer.ts`, `mutations.ts` | `undoableLessonsReducer` owns grouping and the 100-step history; `lessonsReducer` dispatches domain actions; mutations remain pure transforms | `node --import tsx --test tests/unit/lesson-history.test.ts tests/unit/lesson-mutations.test.ts` |
| Preview | `web/src/lib/lesson-builder/use-lesson-preview.ts`, `web/src/app/admin/lesson-builder/page.tsx`, `web/src/app/practice/page.tsx` | `useLessonPreview` owns origin capture, open/close state, preview shape, and focus/selection/scroll restoration; learner entry remains `PracticePage` | `node --import tsx --test tests/unit/lesson-preview.test.ts tests/unit/learner-surfaces.test.ts` |
| Lesson/module movement | `web/src/app/admin/lesson-builder/page.tsx`, `web/src/lib/lesson-builder/use-builder-actions.ts`, `web/src/lib/lesson-builder/use-course-modules.ts`, `web/src/lib/lesson-builder/mutations.ts`, `lesson-file.ts`, `web/src/lib/lesson-builder/builder-context.tsx` | `moveLesson`, `setLessonOrder`, `moduleContainingLesson`, `LessonModule`; `page.tsx` composes `useCourseModules` (module-structure state/handlers: add/delete/move/reorder module, move/create lesson, `getSyllabusMarkers`) and `useBuilderActions` (builds the single `LessonBuilderActions` value, undo/redo, block/piece content mutations) into the value passed to `LessonLibrary`, which stays a plain prop for module-structure moves but drives lesson-content actions via `LessonBuilderProvider`. Module-move handlers (`addModule`/`deleteModule`/`moveModule`/`reorderModule`/`moveLessonToPosition`/`moveLessonToModule`/`patchModule`, all in `use-course-modules.ts`) and `deleteBlock`/`deletePiece`/`moveBlock` (in `use-builder-actions.ts`) are `useCallback`-stable across a typing session (the latter three read current lessons via a `lessonsRef`, not a `[lessons]` dep) — `LessonRow` (`web/src/components/lesson-builder/lesson-library-row.tsx`, `React.memo`) relies on that stability to skip re-rendering collapsed rows on every keystroke | `npm run test:unit -- tests/unit/course.test.ts tests/unit/lesson-mutations.test.ts tests/unit/lesson-library-structure.test.ts` |
| Concept tagging | `web/src/components/lesson-builder/lesson-concepts-field.tsx`, `concept-typeahead.tsx`, `concept-suggestion-chips.tsx`, `concept-quick-edit.tsx` | `LessonConceptsField` is the field shell composing `ConceptTypeahead` (search input + results popover) and `concept-suggestion-chips.tsx`'s `ReviewSuggestions` (module-planning "Suggested review" only — pair-derived Auto-Covers was removed 2026-09-17, see below); `ConceptQuickEdit`, `suggestConceptsForLesson` in `concept-suggestions.ts`. The lesson's own Covers field (`coversFor` set, `hideChips` absent) renders two derived eyebrows, "Introduced"/"Reviewed" (`reviewSplit` prop, from `syllabus.ts`'s `introducedAndReviewedForLesson` via `actions.getLessonReviewSplit`), each its own pill row sharing one `renderChip`/add-input; other callers (module Key concepts, syllabus panel) get the single flat "Covers" eyebrow, unaffected | `npm run test:unit -- tests/unit/concept-suggestions.test.ts tests/unit/lesson-mutations.test.ts tests/unit/syllabus.test.ts` |
| Concept tagging: the Covers typeahead | `web/src/components/lesson-builder/concept-typeahead.tsx`, `lesson-concepts-field.tsx`, `sentence-editor.tsx`, `web/src/lib/lesson-builder/syllabus.ts` | Pair proposals (E3), pair-field autocomplete (E5b), and pair-derived Auto-Covers (E5a) were **removed 2026-09-17** — owner decision, "while writing slides, nothing guesses" (`docs/design/lesson-builder.md` §6); `pair-proposals.ts`, `pair-field-autocomplete.tsx`, and the pair-match half of `concept-suggestions.ts` are gone, a new sentence/vocabulary slide is always blank, and the Spanish/English pair fields in `sentence-editor.tsx` are plain textareas. `ConceptTypeahead` (the Covers field's own search input) stays but got stricter: `Enter`/`Tab` with nothing explicitly arrowed-to links a concept only on an exact (accent/case-insensitive) label match, freehand otherwise; `syllabus.ts`'s `freehandMatchesItem` tolerates a freehand label naming the same syllabus item under that normalisation | `npm run test:unit -- tests/unit/syllabus.test.ts`, `npx playwright test tests/ux/concept-typeahead.spec.ts` |
| Curriculum browsing | `web/src/components/curriculum/curriculum-table.tsx`, `curriculum-table-toolbar.tsx`, `curriculum-table-rows.tsx`, `curriculum-table-sidebar.tsx`, `curriculum-bulk-action-bar.tsx`, `curriculum-pagination.tsx`, `curriculum-browse-panel.tsx`, `curriculum-concept-details.tsx`, `curriculum-row-editor.tsx`, `web/src/lib/curriculum/navigation.ts`, `web/src/lib/curriculum/topics.ts`, `web/src/lib/curriculum/topic-definitions/` | `CurriculumTable` owns state, effects (keyboard shortcuts, focus trapping, URL-driven resets), and composes the pieces below — it renders `CurriculumTableSidebar` (desktop aside + mobile slide-over browse dialog), `CurriculumTableToolbar` (breadcrumb/title, Level-1 progress, module-usage summary, search+filters form, active-filter chips, results count + compact pager + Examples toggle), `CurriculumBulkActionBar` (selection banner), `CurriculumTableRows` (desktop table + mobile card list, including the Spanish/English/Role sort buttons), and `CurriculumPagination` (bottom pager) — all pure presentational components over props/callbacks the table computes; `curriculum-row-editor.tsx` holds `EditableCell`, `CollectionEditor`, `curriculumRoles`, and the editable-field types/helpers; `use-curriculum-navigation.ts` owns URL changes (`navigate`, `goToPage`, `selectTopic/Family/Leaf`, `isNavigating`); `use-curriculum-editing.ts` owns the concept draft, all `/api/admin/curriculum/concepts/:id` mutations, save feedback, and row selection. `buildCurriculumFamilies`, `resolveCurriculumPath`; `topics.ts` is the stable ordered assembly and `findCurriculumTopic` entry point; one topic config per file under `topic-definitions/`; server boundary in `curriculum-store.ts` | `npm run test:unit -- tests/unit/curriculum-navigation.test.ts tests/unit/topic-presentation.test.ts` |
| Learner playback | `web/src/app/practice/page.tsx`, `web/src/components/practice/`, `web/src/lib/learner/progress.ts` | `PracticeMarkdown`, `ExplanationStep`, `SentencePracticeCard`, `LessonSelector`, progress helpers | `npm run test:unit -- tests/unit/learner-surfaces.test.ts tests/unit/learner-progress.test.ts` |
| Learner dashboard | `web/src/app/page.tsx`, `web/src/components/learner/lesson-dashboard.tsx`, `concept-pills.tsx`, `lesson-row.tsx`, `types.ts` | `LessonDashboard` owns module presentation + progress derivation + reset dialog; `ConceptPills` and `LessonRow` are the presentation pieces; shared prop shapes in `types.ts`; progress stays in `lib/learner/progress.ts` | `npm run test:unit -- tests/unit/learner-surfaces.test.ts` |
| Styling | `web/src/app/globals.css`, `web/src/app/layout.tsx`, `web/src/styles/`, `web/src/styles/lesson-builder/`, `web/src/styles/admin/` | Root layout imports globals (tokens/theme/reset only), learner foundations/home, practice base, authoring base, then (since Phase 3a) one lesson-builder stylesheet per component — `lesson-library.css`, `lesson-row.css`, `module-navigator.css`, `lesson-document.css`, `explanation-editor.css`, `sentence-editor.css`, `sentence-presentation.css`, `slide-insert-control.css`, `lesson-script-view.css`, `lesson-concepts-field.css`, `keyboard-help.css`, `editing-hud.css`, `print.css` — then practice/responsive overrides; `site-header.tsx` separately imports `styles/admin/header.css` (moved from `styles/admin-header.css`). One declaration per top-level selector across the whole tree (a 2026-09 pass removed a three-way cascade where `.lesson-session`/`.lesson-topbar`/etc. were declared once each in `learner-foundations-home.css`, `practice-base.css`, and twice in `practice-responsive-overrides.css`, with import/file order silently picking the winner — every property is now merged into the single rule that used to win), geometry (`border-radius`/`overflow`) owned only by a card's outermost element; `!important` is only allowed in `print.css` or inside an `@media (prefers-reduced-motion: reduce)` block. `scripts/lint-css.mjs` (`npm run lint:css`, wired into `npm run lint`) now scans all of `src/styles/**/*.css` plus `src/app/globals.css` (not just `styles/lesson-builder/`) and fails the build on a duplicate top-level selector-list, a stray `!important`, or an unimported stylesheet; search selector consumers with `rg` | `npm run lint && npm run build` |

The Lesson Builder data contract lives in `web/src/lib/lesson-builder/types.ts`; persistence remains `web/data/lessons.json`. Curriculum topic shape lives in `web/src/lib/curriculum/topic-types.ts`, while curriculum roles live in `web/src/lib/curriculum/types.ts` and PostgreSQL reads and writes are under `web/src/lib/curriculum/server/`. For changes to teaching behavior, read [`docs/teaching-methodology.md`](../teaching-methodology.md) before changing the owner above. The Lesson Builder's current design truth is [`docs/design/lesson-builder.md`](../design/lesson-builder.md); archived proposals and superseded verification notes live under `docs/design/archive/`.
